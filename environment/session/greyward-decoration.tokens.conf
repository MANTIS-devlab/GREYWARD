# GREYWARD shared decoration tokens.
# Shared compositor decoration tokens. Application titlebars are native GTK/
# Qt surfaces; these values intentionally stay limited to GREYWARD shell and
# compositor material so there is one visual language without fake chrome.
