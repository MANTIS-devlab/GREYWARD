#!/bin/sh
# The one GREYWARD session-lock entrypoint: keyboard, idle and sleep callers
# all reach the same PAM-backed Wayland locker.
set -eu

if pgrep -u "$(id -u)" -x swaylock >/dev/null 2>&1; then
  exit 0
fi
exec /usr/bin/swaylock --daemonize --color 182027
