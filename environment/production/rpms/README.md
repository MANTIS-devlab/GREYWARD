# Generated production component inputs

The Security Center and security-context RPMs are generated here by the image
factory component-build step. They are inputs to `../provision.sh` and are not
source files or release artifacts. Keep this directory empty in the repository;
the future image builder will populate it before provisioning.
